<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.prctica8guillen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Lon Deng"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ventana1Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PrincipalActivity"
            android:label="Lon Deng"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MesaActivity" />
        <activity android:name=".Mesa_1Activity" />
        <activity android:name=".Mesa_2Activity" />
        <activity android:name=".Mesa_3Activity" />
        <activity android:name=".Mesa_4Activity" />
        <activity android:name=".Mesa_5Activity" />
        <activity android:name=".RegistrarActivity"></activity>
    </application>

</manifest>